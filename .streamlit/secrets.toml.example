# Renombra este archivo a secrets.toml y completa los valores.
# NUNCA subas secrets.toml a git — está en .gitignore.

DATABASE_URL      = "postgresql://user:password@host:5432/inversion_evolutiva?sslmode=require"
DEEPSEEK_API_KEY  = "sk-..."
ALPHA_VANTAGE_API_KEY = "..."
